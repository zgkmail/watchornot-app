# WatchOrNot Backend Environment Configuration

# Server Configuration
PORT=3001
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Security Keys (IMPORTANT: Generate strong random keys!)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_secure_session_secret_here

# API Keys
# Get your API keys from:
# TMDB: https://www.themoviedb.org/settings/api
# Claude: https://console.anthropic.com/ (get API key from Account Settings)
# OMDB: https://www.omdbapi.com/apikey.aspx
TMDB_API_KEY=your_tmdb_api_key_here
CLAUDE_API_KEY=your_claude_api_key_here
OMDB_API_KEY=your_omdb_api_key_here

# Database
# SQLite database will be created at backend/db/watchornot.db in development
# In production (Fly.io), it uses /data/watchornot.db (mounted persistent volume)
# Optional: Override database directory with DATABASE_PATH=/custom/path
# DATABASE_PATH=/data
