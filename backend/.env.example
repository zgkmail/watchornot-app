# CineSense Backend Environment Configuration

# Server Configuration
PORT=3001
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Security Keys (IMPORTANT: Generate strong random keys!)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your_secure_encryption_key_here_min_32_chars
SESSION_SECRET=your_secure_session_secret_here

# Database
# SQLite database will be created at backend/db/cinesense.db
# No additional configuration needed for SQLite
