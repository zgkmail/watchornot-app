# Frontend Environment Variables (Optional)
# These are only needed if deploying to a custom backend URL

# Production Backend URL (optional - defaults to https://watchornot-backend.onrender.com)
# VITE_BACKEND_URL=https://your-custom-backend.com
