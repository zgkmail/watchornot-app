# Development Environment Configuration
# This file is used when running: npm run dev

# Backend URL
# Use deployed backend (has API keys configured)
VITE_BACKEND_URL=https://watchornot-backend.fly.dev

# For local backend testing (uncomment below):
# VITE_BACKEND_URL=http://localhost:3001
# Note: Local dev runs on HTTP. Camera features require HTTPS and must be tested on Fly.io deployment.
