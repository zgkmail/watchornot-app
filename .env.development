# Development Environment Configuration
# This file is used when running: npm run dev

# Backend URL - Use local backend for development
VITE_BACKEND_URL=http://localhost:3001

# Note: Camera features won't work on local HTTP. Test camera on Fly.io deployment.
